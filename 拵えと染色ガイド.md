# 拵え・染色ガイド（色の付け方）

刀の拵え（柄・鍔・頭）、鞘、軍服の**色の付け方**まとめ。
方法は2つ：**大釜で混色**（多彩・ざっくり）／**`/give` の NBT**（厳密・数値指定）。

---

## 1. `/give` で色を指定する（厳密）

色は **10進の RGB 整数**（`0xRRGGBB` を10進化した値）で NBT に入れる。

### 刀（`iron_katana`）— 部位ごと

```
/give @p the_four_primitives_and_weapons:iron_katana{TsukaColor:1783900,TsubaColor:13153866,KashiraColor:16777215}
```

| 部位 | NBTキー | 例の色 |
|------|---------|--------|
| 柄（つか） | `TsukaColor` | `1783900` = `#1B3A5C` |
| 鍔（つば） | `TsubaColor` | `13153866` = `#C8B34A` |
| 頭（かしら） | `KashiraColor` | `16777215` = `#FFFFFF` |

- 指定した部位だけ色が付き、未指定の部位は**元の紺の菱デザイン**のまま。
- **持っている刀**も**納刀した鞘**も同じ見た目で反映される。

### 鞘（`saya` / `tyokuto_saya` / `sword_saya` / `rapier_saya`）— 地色

```
/give @p the_four_primitives_and_weapons:saya{SayaBase:8000034}
```

| NBTキー | 意味 |
|---------|------|
| `SayaBase` | 鞘の地色（10進RGB） |

- `display.color` でも可（革装備方式）：`saya{display:{color:8000034}}`

### 軍服（上着）— `imperial_army_tunic` など

```
/give @p the_four_primitives_and_weapons:imperial_army_tunic{display:{color:16711680}}
```

- 革装備と同じ `display:{color:N}`（10進RGB）。従来の `GloveColor:N` でも可。
- 対象：`imperial_army_tunic` / `battoutai_tunic` / `meiji_police_tunic` / `keishi_patrol_tunic` / `keishi_officer_tunic`

---

## 2. 16進 → 10進 の変換

`/give` の NBT int は**10進**なので、`#RRGGBB` を10進に直して入れる。

| 16進 | 10進 |
|------|------|
| `#000000`（黒） | `0` |
| `#FFFFFF`（白） | `16777215` |
| `#FF0000`（赤） | `16711680` |
| `#1B3A5C`（紺） | `1783900` |
| `#C8B34A`（金） | `13153866` |

変換：`R*65536 + G*256 + B`（各成分は10進0〜255）。

---

## 3. 大釜で混色（多彩・ほぼ無限）

1. 大釜を水で満たす。
2. **染料を複数入れる** → 水が色付き、**革防具と同じ混色**で混ざる（濁らず鮮やか）。
3. 色付きの水に **刀 / 軍服 / 鞘** を右クリックで浸すと、その色に染まる。
   - **刀**：柄・鍔・頭を**一気に同色**に。
   - **鞘**：地色を染める。
   - **軍服**：上着を染める。
4. **ただの水**に浸すと色が落ちる（洗う）。
5. 水バケツで満たし直すと色リセット。

> 部位ごとに別々の色にしたい場合は `/give`（上記）か **拵え台**（染料スロットで部位ごと）を使う。

---

## 4. 仕組み（メモ）

- 色を設定した部位は、内部で**グレー版テクスチャ**に差し替えて色を乗算 → 模様を保ったまま任意色が出る（軍服の染色と同じ）。
- **ほぼ黒**の時だけ、乗算では潰れるので**模様入りの暗版テクスチャ**に差し替える（黄色は出さない）。
- 未設定＝元の紺の菱デザイン。

関連テクスチャ：`textures/katana_fitting/{tsuka,tsuba,kasira}/` の `tuka` / `tuka_gray` / `tuka_black` など。
テクスチャの描き方は同フォルダの `README.md` を参照。
